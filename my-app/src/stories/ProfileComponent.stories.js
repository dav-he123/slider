import React from "react";
import { action } from "@storybook/addon-actions";

import Profile from "../components/Profile/ProfileIndex";

export default {
  title: "Profile",
  component: Profile,
};

export const ProfileComp = () => <Profile />;
